From 9b59406eca2365ec01f3639f0951092ac9e67d67 Mon Sep 17 00:00:00 2001
From: Charles Mirabile <cmirabil@redhat.com>
Date: Thu, 1 May 2025 01:07:30 -0400
Subject: [PATCH 50/74] fixup! drivers: pwm: Added ESWIN pwm driver

combine declaration and initialization
---
 drivers/pwm/pwm-eswin.c | 3 +--
 1 file changed, 1 insertion(+), 2 deletions(-)

diff --git a/drivers/pwm/pwm-eswin.c b/drivers/pwm/pwm-eswin.c
index 57cdbe8e09d9..74d750ed0afe 100644
--- a/drivers/pwm/pwm-eswin.c
+++ b/drivers/pwm/pwm-eswin.c
@@ -209,12 +209,11 @@ MODULE_DEVICE_TABLE(of, eswin_pwm_dt_ids);
 static int eswin_pwm_probe(struct platform_device *pdev)
 {
 	struct eswin_pwm *pc;
-	struct pwm_chip *chip;
 	int ret, count;
 	struct resource *res;
 	int clk_rate;
 
-	chip = devm_pwmchip_alloc(&pdev->dev, NUM_PWM_CHANNEL, sizeof(*pc));
+	struct pwm_chip *chip = devm_pwmchip_alloc(&pdev->dev, NUM_PWM_CHANNEL, sizeof(*pc));
 	if(IS_ERR(chip))
 		return PTR_ERR(chip);
 
-- 
2.53.0

